function Pfa = binomial_false_alarm(pfa,n)
    % ---------------------------------------------------------------------    
    % coherent_gain: Returns the gain associated with coherently
    %                   integrating N pulses
    % ---------------------------------------------------------------------
    % Usage:
    %  Inputs
    %   > pfa:    probability of false alarm for single pulse
    %   >   n:    number of pulses transmitted
    %
    %  Outputs
    %   > gain: decibel gain due to N coherently integrated pulses
    %
    % ---------------------------------------------------------------------
    
    gain = 10*log(N);
    
end